// 1) Math.abs(x) → Gives the positive value.

// 2) Math.round(x) → Rounds to the nearest whole number.

// 3) Math.floor(x) → Rounds down to the next lower number.

// 4) Math.ceil(x) → Rounds up to the next higher number.

// 5) Math.max(a,b,...) → Finds the biggest number.

// 6) Math.min(a,b,...) → Finds the smallest number.

// 7) Math.sqrt(x) → Finds the square root.

// 8) Math.pow(a,b) → Raises a number to a power.

// 9) Math.random() → Gives a random number between 0 and 1.


let num = -10;

// console.log(Math.abs(num));
// console.log(Math.round(5.6+))
console.log(Math.floor(5.6))
console.log(Math.ceil(5.6))
console.log(Math.max(9,5,6,7,4,3,2))
console.log(Math.min(9,5,6,7,4,3,2))
console.log(Math.sqrt(100))
console.log(Math.pow(2,2))